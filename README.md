# Form Extractor Prototype Examples


## Run

Start the app locally with `npm start dev`.

It'll be available at http://localhost:3001/

Update the Sass with `sass assets/style.scss public/assets/style.css`.
